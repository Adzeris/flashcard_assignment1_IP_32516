# Copy to .env.local for local testing against a remote API (optional).
# On Vercel: Project → Settings → Environment Variables → VITE_API_BASE
# Use your Render URL with NO trailing slash, e.g. https://flashcards-api-xxxx.onrender.com
VITE_API_BASE=
